#include <stdio.h>
#include "main.h"

/**
 * puts_half - prints half of a string and a new line
 * @str: pointer variables
 */

void puts_half(char *str)
{
	int i, count = 0;

	while (count >= 0

	{
	}

}
