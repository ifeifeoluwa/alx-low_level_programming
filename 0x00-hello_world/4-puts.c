#include <stdio.h>

/**
 * main - Entry point
 * Prints on screen
 * Return - Always 0 (Success)
 */

int main(void)
{
	char str[80] =  "\"Programming is like building a multilingual puzzle\"";

	puts(str);
	return (0);
}
