#include <stdio.h>

/**
 * main - Entry point
 *
 * This program prints on screen
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char str[80] =  "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
