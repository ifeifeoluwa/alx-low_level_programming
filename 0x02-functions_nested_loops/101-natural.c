#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the sum of all multiples if 3 and 5 that is less than 1024
 *
 * Return: returns 0
 */

int main(void)
{
	int a = 0, b = 0;

	while (a < 1024)
	{
		if (a % 3 == 0 || c % 5 == 0)
		{
			b += a;
		}
		a++;
	}
	printf("%i\n", c);
	return (0);
}
