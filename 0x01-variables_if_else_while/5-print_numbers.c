#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int base_ten;

	for (base_ten = 0; base_ten < 10; base_ten++)
	{
		printf("%d", base_ten);
	}
	printf("\n");
	return (0);
}
